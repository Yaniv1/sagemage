Input: a set of rows with fields item_id, name, type.

{$DATA$}

If an item has multiple types - select the likeliest type for the item.
Provide a summary listing each `type` with its `items` count and `share` (fraction of total).
Output under the JSON node `results` as an array of objects with keys: type, items, share.
Return only the valid JSON response with a `results` root node and no additional comments.

{$RESOURCES$}